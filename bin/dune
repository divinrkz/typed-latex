(copy_files "../tex/*.tex")

(executable
 (public_name typed_latex)
 (name main)
 (libraries typed_latex core stdio re))

; Avoid program hault on warnings
(env (dev (flags (:standard -warn-error -A))))
