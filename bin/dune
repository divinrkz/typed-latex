; (copy_files "../assets/json/*.json")

(executable
 (public_name typed_latex)
 (name main)
 (libraries typed_latex core stdio re spectrum))

; Avoid program hault on warnings
(env (dev (flags (:standard -warn-error -A))))

